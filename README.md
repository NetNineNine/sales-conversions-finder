# sales-conversions-finder

Finds services that have gone active to calculate sales agent commission

## To run the application

```sh
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
./app.py
```
